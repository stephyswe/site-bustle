export const InputEmail = ({ className, placeholder }: any) => (
  <input
    onChange={() => {}}
    autoComplete="email"
    id="email-signup"
    name="email"
    placeholder="email@email.com"
    required={true}
    type="email"
    value=""
    className="oAn"
  />
);
